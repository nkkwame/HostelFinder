# MongoDB Connection
# Replace with your actual MongoDB Atlas connection string
# Format: mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://YOUR_USERNAME:YOUR_PASSWORD@YOUR_CLUSTER.mongodb.net/YOUR_DATABASE?retryWrites=true&w=majority

# Server Configuration
PORT=5000
NODE_ENV=development

# Instructions:
# 1. Replace YOUR_USERNAME with your MongoDB Atlas username
# 2. Replace YOUR_PASSWORD with your MongoDB Atlas password
# 3. Replace YOUR_CLUSTER with your actual cluster name
# 4. Replace YOUR_DATABASE with your database name
# 5. Save this file as .env (remove the .template extension)
# 6. Add .env to your .gitignore file to keep credentials secure
